{% extends 'base.html.twig' %}

{% block title %}Edit Books{% endblock %}

{% block body %}
    <h1>Edit Books</h1>

    {{ include('books/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('books_index') }}" class="btn btn-light">back to list</a>

    {{ include('books/_delete_form.html.twig') }}
{% endblock %}
